---
# DO NOT MODIFY THIS PLAYBOOK - exam requirement
# Only edit templates/hosts.j2
- name: Generate /etc/myhosts on dev hosts
  hosts: dev
  tasks:
    - name: Deploy hosts file from template
      ansible.builtin.template:
        src: hosts.j2
        dest: /etc/myhosts
